@model infrastructurre.DTO.SaleProductDTO;

@{
    ViewData["Title"] = "Create";
}
<!DOCTYPE html>
<html lang="en">

<head>

</head>

<body>
    <div class="Sale">
    </div><!-- End Page Title -->
    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">sale</h5>
                        @using (Html.BeginForm(FormMethod.Post, new { @Class = "row g-3" }))
                        {
                            @* <div class="    col-md-4">
                                <label for="inputText" class="form-label">ProductName</label>
                                @Html.TextBoxFor(a => a.ProductName, new { type = "text", placeholder = "Name", @Class = "form-control", required = "required" })
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(a => a.ProductName, null, new { @Class = "text-danger" })
                                </div>

                            </div>
                            <div class="    col-md-4">
                                <label for="inputText" class="form-label">Date</label>
                                @Html.TextBoxFor(a => a.SaleDate, new { placeholder = "Date", @Class = "form-control", required = "required" })
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(a => a.SaleDate, null, new { @Class = "text-danger" })
                                </div>

                            </div>
                            <div class="    col-md-4">
                                <label for="inputText" class="form-label">Quantity</label>
                                @Html.TextBoxFor(a => a.Quantity, new { type = "text", placeholder = "Quantity", @Class = "form-control", required = "required" })
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(a => a.Quantity, null, new { @Class = "text-danger" })
                                </div>

                            </div>
                            <div class="    col-md-4">
                                <label for="inputText" class="form-label">TotalAmount</label>
                                @Html.TextBoxFor(a => a.TotalAmount, new { type = "text", placeholder = "Amount", @Class = "form-control", required = "required" })
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(a => a.TotalAmount, null, new { @Class = "text-danger" })
                                </div>

                            </div> *@


                            <div class="col-15">
                                <button class="btn btn-primary" type="submit">Update</button>
                            </div>



                        }
                    </div>
                </div>
            </div>
        </div>
    </section>


    <a href="#" class="back-to-top d-flex align-items-center justify-content-center"><i class="bi bi-arrow-up-short"></i></a>

</body>

</html>