@model infrastructurre.DTO.CategoryDTO
@{
    ViewData["Title"] = "Create";
}
<!DOCTYPE html>
<html lang="en">

<head>
</head>

<body>
    <div class="Update category">
    </div><!-- End Page Title -->
    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Update Category</h5>
                        @using (Html.BeginForm(FormMethod.Post, new { @Class = "row g-3" }))
                        {
                          @*   <div class="col-md-4">
                                <label for="inputText" class="form-label">CategoryID</label>
                                @Html.TextBoxFor(a => a.Categoryid, new { type = "text", placeholder = "ID", @Class = "form-control", required = "required" })
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(a => a.Categoryid, null, new { @Class = "text-danger" })
                                </div>

                            </div> *@
                            
                            @Html.HiddenFor(x=>x.Id);

                            <div class="col-md-4">
                                <label for="inputText" class="form-label">CustomerName</label>
                                @Html.TextBoxFor(a => a.Categoryname, new { placeholder = "Name", @Class = "form-control", required = "required" })
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(a => a.Categoryname, null, new { @Class = "text-danger" })
                                </div>

                            </div>

                            <div class="col-12">
                                <button class="btn btn-primary" type="submit">Update form</button>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </section>


    <a href="#" class="back-to-top d-flex align-items-center justify-content-center"><i class="bi bi-arrow-up-short"></i></a>

</body>

</html>
